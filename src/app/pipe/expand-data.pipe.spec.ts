import { ExpandDataPipe } from './expand-data.pipe';

describe('ExpandDataPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpandDataPipe();
    expect(pipe).toBeTruthy();
  });
});
