import { SexFormatPipe } from './sex-format.pipe';

describe('SexFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SexFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
